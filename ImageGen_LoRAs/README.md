# Image generation LoRAs

Drop LoRA `.safetensors` files here. They'll appear in the **Image generation** pane and can be stacked on top of a base model in `../ImageGen_Models/`.

Civitai is the typical source. Make sure the LoRA family (SD 1.5 / SDXL / Flux) matches your base model.
